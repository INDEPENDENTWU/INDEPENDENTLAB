window.DINNER_CHOICE_VIEW=({stage,answers,next,buzz},step)=>{const selected=answers[step.id]?.value;stage.innerHTML=`<div class="panel"><h1 class="question">${step.title}</h1><div class="choices">${step.options.map(o=>`<button class="choice${selected===o.value?' selected':''}" type="button" data-value="${o.value}">${o.label}</button>`).join('')}</div></div>`;stage.querySelectorAll('.choice').forEach(b=>b.onclick=()=>{answers[step.id]=step.options.find(o=>o.value===b.dataset.value);buzz();stage.querySelectorAll('.choice').forEach(x=>x.classList.toggle('selected',x===b));setTimeout(next,155)})};