window.DINNER_RANGE_VIEW=({stage,answers,buzz},step,finish)=>{const value=answers[step.id]?.value??50;stage.innerHTML=`<div class="panel"><h1 class="question">${step.title}</h1><div class="desire"><input class="range" id="desire" type="range" min="0" max="100" value="${value}" aria-label="想去程度" style="--fill:${value}%"><div class="range-labels"><span>完全不想</span><span>很想去</span></div><div class="continue-row"><button class="primary" id="decide" type="button">就这样</button></div></div></div>`;const range=stage.querySelector('#desire');range.oninput=()=>range.style.setProperty('--fill',`${range.value}%`);stage.querySelector('#decide').onclick=()=>{answers[step.id]={value:Number(range.value)};buzz(18);finish()}};