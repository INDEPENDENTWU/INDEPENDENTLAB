window.EGG_MOTION={frame(video,canvas,ctx){if(video.readyState<2)return null;ctx.drawImage(video,0,0,canvas.width,canvas.height);const d=ctx.getImageData(0,0,canvas.width,canvas.height).data,out=new Float32Array(canvas.width*canvas.height);for(let i=0,j=0;i<d.length;i+=4,j++)out[j]=.299*d[i]+.587*d[i+1]+.114*d[i+2];return out},score(cur,old,w,h){if(!cur||!old)return 0;const cx=.5*w,cy=.47*h,rx=.23*w,ry=.30*h;let a=0,b=0,an=0,bn=0;for(let y=1;y<h-1;y+=2)for(let x=1;x<w-1;x+=2){const i=y*w+x,inside=((x-cx)/rx)**2+((y-cy)/ry)**2<=1,d=cur[i]-old[i];if(inside){a+=d;an++}else if(x>5&&x<w-6&&y>5&&y<h-6){b+=d;bn++}}const da=a/Math.max(1,an),db=b/Math.max(1,bn);a=b=an=bn=0;for(let y=1;y<h-1;y+=2)for(let x=1;x<w-1;x+=2){const i=y*w+x,inside=((x-cx)/rx)**2+((y-cy)/ry)**2<=1;if(inside){a+=Math.abs((cur[i]-old[i])-da);an++}else if(x>5&&x<w-6&&y>5&&y<h-6){b+=Math.abs((cur[i]-old[i])-db);bn++}}return Math.max(0,a/Math.max(1,an)-(b/Math.max(1,bn))*.55)}};