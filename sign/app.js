(()=>{const $=s=>document.querySelector(s),message=$('#message'),raise=$('#raise'),clear=$('#clear'),previewText=$('#previewText'),board=$('#board'),boardText=$('#boardText'),boardBack=$('#boardBack'),boardTone=$('#boardTone'),installHint=$('#installHint'),themeColor=$('#themeColor');const textKey='sign-last-text',toneKey='sign-tone',readStore=(k,f='')=>{try{return localStorage.getItem(k)??f}catch{return f}},writeStore=(k,v)=>{try{localStorage.setItem(k,v)}catch{}};let displaying=false,wake=null,tone=Math.max(0,Math.min(2,Number(readStore(toneKey,'0'))||0)),chromeTimer=0;const ios=/iPhone|iPod/i.test(navigator.userAgent),standalone=()=>window.navigator.standalone===true||matchMedia('(display-mode: standalone)').matches||matchMedia('(display-mode: fullscreen)').matches;installHint.hidden=!(ios&&!standalone());document.documentElement.classList.toggle('installed',standalone());function fit(el,min,max){let lo=min,hi=max,best=min;el.style.fontSize=`${min}px`;for(let i=0;i<11;i++){const mid=(lo+hi)/2;el.style.fontSize=`${mid}px`;if(el.scrollHeight<=el.clientHeight+1&&el.scrollWidth<=el.clientWidth+1){best=mid;lo=mid}else hi=mid}el.style.fontSize=`${Math.floor(best)}px`}function fitAll(){requestAnimationFrame(()=>{fit(previewText,18,56);if(displaying)fit(boardText,24,Math.min(innerWidth,innerHeight)*.5)})}function paint(){const v=message.value.trim();previewText.textContent=v||'写一句话';raise.disabled=!v;clear.hidden=!v;fitAll()}async function acquireWake(){if(!displaying||document.hidden||wake||!navigator.wakeLock?.request)return;try{const lock=await navigator.wakeLock.request('screen');if(!displaying){await lock.release();return}wake=lock;lock.addEventListener('release',()=>{wake=null;if(displaying&&!document.hidden)setTimeout(acquireWake,220)})}catch{}}async function releaseWake(){const current=wake;wake=null;if(current)try{await current.release()}catch{}}function applyTone(){board.classList.remove('tone-0','tone-1','tone-2');board.classList.add(`tone-${tone}`);const colors=['#f2efe7','#10110f','#f0c800'];if(themeColor)themeColor.content=colors[tone];writeStore(toneKey,String(tone))}function scheduleChrome(){clearTimeout(chromeTimer);board.classList.remove('clean');chromeTimer=setTimeout(()=>{if(displaying)board.classList.add('clean')},1500)}async function enterFullscreen(){if(standalone())return;const target=board,request=target.requestFullscreen||target.webkitRequestFullscreen;if(!request)return;try{await request.call(target,{navigationUI:'hide'})}catch{try{await request.call(target)}catch{}}}async function leaveFullscreen(){try{if(document.fullscreenElement&&document.exitFullscreen)await document.exitFullscreen();else if(document.webkitFullscreenElement&&document.webkitExitFullscreen)await document.webkitExitFullscreen()}catch{}}async function enter(){const v=message.value.trim();if(!v)return;displaying=true;boardText.textContent=v;board.hidden=false;document.body.style.overflow='hidden';applyTone();fitAll();acquireWake();await enterFullscreen();scheduleChrome();setTimeout(fitAll,120)}async function leave(){displaying=false;clearTimeout(chromeTimer);board.classList.remove('clean');await releaseWake();board.hidden=true;document.body.style.overflow='';await leaveFullscreen();if(themeColor)themeColor.content='#141411';setTimeout(()=>message.focus({preventScroll:true}),80)}message.addEventListener('input',()=>{writeStore(textKey,message.value);paint()});raise.onclick=enter;clear.onclick=()=>{message.value='';writeStore(textKey,'');paint();message.focus()};boardBack.onclick=e=>{e.stopPropagation();leave()};boardTone.onclick=e=>{e.stopPropagation();tone=(tone+1)%3;applyTone();scheduleChrome()};board.addEventListener('pointerdown',e=>{if(e.target===boardBack||e.target===boardTone)return;if(board.classList.contains('clean'))scheduleChrome();else{clearTimeout(chromeTimer);board.classList.add('clean')}});document.addEventListener('visibilitychange',()=>{if(!document.hidden&&displaying){setTimeout(acquireWake,90);setTimeout(fitAll,120);scheduleChrome()}});window.addEventListener('resize',fitAll);window.addEventListener('orientationchange',()=>setTimeout(fitAll,160));document.addEventListener('fullscreenchange',()=>{if(displaying)setTimeout(fitAll,80)});document.addEventListener('webkitfullscreenchange',()=>{if(displaying)setTimeout(fitAll,80)});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&displaying)leave()});message.value=readStore(textKey,'');paint();applyTone()})();