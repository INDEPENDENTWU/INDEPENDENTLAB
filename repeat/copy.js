(()=>{'use strict';const clean=()=>{const caught=document.getElementById('caughtCount');if(caught?.textContent.includes('不会因为出现两次'))caught.textContent='';const heard=document.getElementById('lastHeard');if(heard?.textContent.startsWith('听到了，但这段没有形成稳定文字：'))heard.textContent=`没听清：${heard.textContent.split('：').slice(1).join('：')}`;if(heard?.textContent==='会显示浏览器识别到的文字，方便你判断它有没有听准。')heard.textContent='';const state=document.getElementById('listenState');if(state?.textContent.startsWith('已适配 · '))state.textContent=state.textContent.replace('已适配 · ','');document.querySelectorAll('.ranking .empty').forEach(el=>{if(el.textContent.includes('主题词出现很多次'))el.textContent='继续说。'});const meta=document.getElementById('resultMeta');if(meta?.textContent.includes('暂时没有足够明显的语言习惯'))meta.textContent=meta.textContent.replace('暂时没有足够明显的语言习惯','还没有明显重复');document.querySelectorAll('#resultList li span').forEach(el=>{if(el.childNodes[0]?.nodeType===3&&el.childNodes[0].textContent.trim()==='这次没有明显重复')el.childNodes[0].textContent='没有明显重复'});const label=document.getElementById('caughtLabel');if(label?.textContent==='还在找习惯')label.textContent='还没明显重复'};clean();new MutationObserver(clean).observe(document.body,{subtree:true,childList:true,characterData:true})})();