(()=>{'use strict';const $=s=>document.querySelector(s),home=$('#home'),start=$('#start'),error=$('#error'),camera=$('#camera'),exit=$('#exit'),video=$('#video'),overlay=$('#overlay'),watch=$('#watch'),stateSmall=$('#stateSmall'),stateTitle=$('#stateTitle'),instruction=$('#instruction'),meter=$('#meter'),result=$('#result'),resultBack=$('#resultBack'),again=$('#again'),redo=$('#redo'),verdict=$('#verdict'),resultText=$('#resultText'),confidence=$('#confidence'),shot=$('#shot'),sctx=shot.getContext('2d'),hotspot=$('#hotspot'),hotspot2=$('#hotspot2'),sample=$('#sample'),ctx=sample.getContext('2d',{willReadFrequently:true});const GW=12,GH=9,N=GW*GH;let stream=null,raf=0,mode='idle',prev=null,noise=1.2,baseSamples=[],heat=new Float32Array(N),startedAt=0,lastMotionAt=0,motionStarted=false,peak=0,peakFrame=null,globalBadAt=0;const now=()=>performance.now(),clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),median=a=>{if(!a.length)return 0;const s=[...a].sort((x,y)=>x-y);return s[s.length>>1]};function setState(a,b,p){stateSmall.textContent=a;stateTitle.textContent=b;if(p!=null)instruction.textContent=p}function stop(){cancelAnimationFrame(raf);raf=0;if(stream){stream.getTracks().forEach(t=>t.stop());stream=null}video.srcObject=null;prev=null}async function open(){error.hidden=true;try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:960},frameRate:{ideal:30,min:15}},audio:false});video.srcObject=stream;await video.play();home.hidden=true;result.hidden=true;camera.hidden=false;mode='idle';setState('准备','把手机放稳','让要看的线头都留在画面里。');watch.textContent='开始盯'}catch(e){console.error(e);error.hidden=false;error.textContent='没有打开相机。允许相机权限后再试。';stop()}}function frame(){if(video.readyState<2)return null;ctx.drawImage(video,0,0,sample.width,sample.height);const d=ctx.getImageData(0,0,sample.width,sample.height).data,g=new Uint8Array(sample.width*sample.height);for(let i=0,j=0;i<d.length;i+=4,j++)g[j]=(d[i]*3+d[i+1]*6+d[i+2])>>3;return g}function diffGrid(cur){if(!prev){prev=cur;return null}const cells=new Float32Array(N),counts=new Uint16Array(N),cw=sample.width/GW,ch=sample.height/GH;for(let y=2;y<sample.height-2;y+=2){const gy=Math.min(GH-1,(y/ch)|0);for(let x=2;x<sample.width-2;x+=2){const gx=Math.min(GW-1,(x/cw)|0),ci=gy*GW+gx,idx=y*sample.width+x;cells[ci]+=Math.abs(cur[idx]-prev[idx]);counts[ci]++}}for(let i=0;i<N;i++)cells[i]/=Math.max(1,counts[i]);prev=cur;return cells}function localized(cells){const g=median(Array.from(cells)),out=new Float32Array(N);let sum=0,hot=0;const th=Math.max(noise*1.35+1.1,g*1.45+1);for(let i=0;i<N;i++){const v=Math.max(0,cells[i]-g*.9-th*.38);out[i]=v;sum+=v;if(cells[i]>th*1.7)hot++}return{out,sum,global:g,spread:hot/N}}function begin(){if(!stream)return;mode='cal';prev=null;baseSamples=[];heat.fill(0);motionStarted=false;startedAt=now();lastMotionAt=0;peak=0;peakFrame=null;globalBadAt=0;watch.disabled=true;watch.textContent='正在盯';setState('先别动','记住这堆线','手机放稳 1 秒，先不要碰线。');cancelAnimationFrame(raf);raf=requestAnimationFrame(loop)}function capturePeak(){const c=document.createElement('canvas'),maxW=1000,scale=Math.min(1,maxW/video.videoWidth);c.width=Math.max(1,Math.round(video.videoWidth*scale));c.height=Math.max(1,Math.round(video.videoHeight*scale));c.getContext('2d').drawImage(video,0,0,c.width,c.height);peakFrame=c}function loop(){if(!stream||camera.hidden)return;const t=now(),cur=frame();if(!cur){raf=requestAnimationFrame(loop);return}const cells=diffGrid(cur);if(!cells){raf=requestAnimationFrame(loop);return}if(mode==='cal'){baseSamples.push(median(Array.from(cells)));meter.style.width='8%';if(t-startedAt>1050){noise=Math.max(.7,median(baseSamples));mode='watch';startedAt=t;setState('现在','去另一头轻拽','轻拽要找的那根 2–3 下，手机不要动。');meter.style.width='18%'}}else{const m=localized(cells);if(m.spread>.52&&m.global>noise*2.2+2.2){if(!globalBadAt)globalBadAt=t;if(t-globalBadAt>260){setState('手机动了','先把手机放稳','画面稳定后会继续盯。');heat.fill(0);motionStarted=false;lastMotionAt=0}}else{if(globalBadAt&&t-globalBadAt>300){baseSamples=[];startedAt=t;mode='cal';prev=null;globalBadAt=0;setState('重新准备','记住这堆线','手机放稳 1 秒。');raf=requestAnimationFrame(loop);return}globalBadAt=0;const eventTh=Math.max(12,noise*4.2+5);meter.style.width=`${clamp(18+m.sum/(eventTh*5)*72,18,92)}%`;if(m.sum>eventTh){if(!motionStarted){motionStarted=true;setState('看到了','继续轻拽','再轻拽 1–2 下，会自动停。')}lastMotionAt=t;for(let i=0;i<N;i++)heat[i]+=m.out[i];if(m.sum>peak){peak=m.sum;capturePeak()}}if(motionStarted&&lastMotionAt&&t-lastMotionAt>950){finish();return}if(t-startedAt>12000){finish();return}}}raf=requestAnimationFrame(loop)}function smoothHeat(src){const out=new Float32Array(N);for(let y=0;y<GH;y++)for(let x=0;x<GW;x++){let s=0,w=0;for(let yy=Math.max(0,y-1);yy<=Math.min(GH-1,y+1);yy++)for(let xx=Math.max(0,x-1);xx<=Math.min(GW-1,x+1);xx++){const k=1/(1+Math.abs(xx-x)+Math.abs(yy-y));s+=src[yy*GW+xx]*k;w+=k}out[y*GW+x]=s/w}return out}function topRegions(h){let a=-1,av=-1;for(let i=0;i<N;i++)if(h[i]>av){av=h[i];a=i}let b=-1,bv=-1,ax=a%GW,ay=(a/GW)|0;for(let i=0;i<N;i++){const x=i%GW,y=(i/GW)|0;if(Math.abs(x-ax)<=2&&Math.abs(y-ay)<=2)continue;if(h[i]>bv){bv=h[i];b=i}}return{a,av,b,bv,ratio:av/Math.max(1,bv)}}function drawSnapshot(){if(!peakFrame)return;shot.width=peakFrame.width;shot.height=peakFrame.height;sctx.drawImage(peakFrame,0,0)}function place(el,idx){const x=idx%GW,y=(idx/GW)|0;el.style.left=`${(x+.5)/GW*100}%`;el.style.top=`${(y+.5)/GH*100}%`}function finish(){cancelAnimationFrame(raf);raf=0;const h=smoothHeat(heat),r=topRegions(h),enough=motionStarted&&r.av>Math.max(5,noise*3);camera.hidden=true;result.hidden=false;drawSnapshot();if(!enough){verdict.textContent='没抓到';resultText.textContent='刚才没有出现足够明显的局部动作。';confidence.textContent='再轻拽一次';hotspot.hidden=true;hotspot2.hidden=true}else if(r.ratio<1.34&&r.b>=0){verdict.textContent='两处都动了';resultText.textContent='这次有两处一起明显移动，再拽一次更容易分清。';confidence.textContent='不够唯一';hotspot.hidden=false;place(hotspot,r.a);hotspot2.hidden=false;place(hotspot2,r.b)}else{verdict.textContent='这里动了';resultText.textContent='画面里这一处的动作最明显。';confidence.textContent=r.ratio>1.9?'很清楚':'比较清楚';hotspot.hidden=false;place(hotspot,r.a);hotspot2.hidden=true}stop()}start.onclick=open;watch.onclick=begin;exit.onclick=()=>{stop();camera.hidden=true;home.hidden=false};resultBack.onclick=()=>{stop();result.hidden=true;home.hidden=false};again.onclick=async()=>{result.hidden=true;await open();setTimeout(begin,250)};redo.onclick=()=>{result.hidden=true;home.hidden=false};window.addEventListener('pagehide',stop)})();